services:
  - type: web
    name: watchdog-laravel
    runtime: docker
    plan: free
    rootDir: src
    buildCommand: |
      # Optional: pre-build steps
      echo "Docker build will handle dependencies"
    envVars:
      # ... same as before, without startCommand

  - type: worker
    name: watchdog-worker
    runtime: docker
    plan: free
    rootDir: src
    buildCommand: |
      echo "Docker build handles dependencies"
    envVars:
      # ... same as before
